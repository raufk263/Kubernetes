kind: Service
apiVersion: v1
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80              # Port of my host system
      targetPort: 80        # Port of my application
  type: ClusterIP
